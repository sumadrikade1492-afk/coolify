## Packages
framer-motion | Smooth animations for page transitions and interactive elements
react-hook-form | Form state management
@hookform/resolvers | Zod resolver for react-hook-form

## Notes
Authentication uses Replit Auth:
- Login URL: /api/login
- Logout URL: /api/logout
- User data: useAuth hook (already generated in context)

Images:
- Profile photos will be URLs (either user uploaded or placeholders)
- Use Unsplash for decorative images (hero, backgrounds)
